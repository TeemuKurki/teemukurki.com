
<article class="card blog-card" >
  <img alt="{{blog.title}} image" src={{blog.image}} transform-images="avif webp jpeg 256" />
  <a href={{blog.url}} class="h3 title action">{{blog.title}}</a>
  <p class="lead-text">{{blog.leadText}}</p>
</article>  